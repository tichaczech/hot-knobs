<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <!-- Naming convention -->
  <PropertyGroup>
    <DefaultRootNamespace>$(DefaultRootNamespace).Storage</DefaultRootNamespace>
  </PropertyGroup>

</Project>
