<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <!-- Naming convention -->
  <PropertyGroup>
    <DefaultRootNamespace>$(DefaultRootNamespace).Validation</DefaultRootNamespace>
  </PropertyGroup>

</Project>
